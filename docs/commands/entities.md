<!-- Generated by `secopsctl docs generate` — do not edit by hand. -->

# secopsctl entities

Inspect entities: summarize alerts, prevalence, and findings-graph pivot

## entities audit

Read-only: watchlist health + high-risk entities by risk score

```text
secopsctl entities audit [--min-risk N] [--limit N] [flags]
```

```text
Audit detection posture: watchlist inventory + health (empty lists,
default multiplying factors) and the entities at or above the risk
threshold, with the risk-score distribution. Per-entity watchlist
membership is not cross-referenced; treat the high-risk list as
candidates to verify against watchlists.

All reads are API-only — no mutations.
```

**Flags**

| Flag | Type | Default | Description |
|---|---|---|---|
| `--limit` | int | 200 | max risk-score rows to fetch |
| `--min-risk` | int | 500 | minimum risk score to flag as high-risk (0-1000) |

## entities graph

Read-only: seed a findings graph from a detection (entities, edges, lateral movement)

```text
secopsctl entities graph <detection-id> [flags]
```

```text
Initialize the findings graph from a detection id over the last --hours: the
root node plus the first page of connected nodes and edges — the entity-to-
entity pivot behind the console's graph investigation view. Expand a node with
`entities graph explore` using the node ids from this response (same window).
```

**Flags**

| Flag | Type | Default | Description |
|---|---|---|---|
| `--hours` | int | 168 | look-back window in hours (node ids are tied to this range) |

## entities graph explore

Read-only: expand a node of an initialized findings graph

```text
secopsctl entities graph explore --param key=value ... [flags]
```

```text
Expand one node (or unpack a group node) of a graph seeded by `entities graph`.
The node ids and the exact query-param names come from that response and must
use the SAME time range; pass them verbatim with repeatable --param key=value.
```

**Flags**

| Flag | Type | Default | Description |
|---|---|---|---|
| `--param` | stringArray | - | query parameter key=value (repeatable; from the `entities graph` response) |

## entities risk-scores

Read-only: list per-entity behavioral risk scores (filter + sort)

```text
secopsctl entities risk-scores [--filter EXPR] [--order-by FIELD] [--limit N] [flags]
```

```text
Query entityRiskScores — a normalized behavioral risk score per entity, the
signal for prioritizing which hosts/users to look at first. --filter and
--order-by pass through to the API (e.g. --order-by 'riskScore desc'); --limit
caps the page. JSON output.
```

**Flags**

| Flag | Type | Default | Description |
|---|---|---|---|
| `--filter` | string | - | server-side filter expression |
| `--limit` | int | 100 | max rows to return |
| `--order-by` | string | - | order-by field, e.g. "riskScore desc" |

## entities summarize

Read-only: summarize an entity (alerts, related, prevalence) over a window

```text
secopsctl entities summarize <entity-type> <value> [flags]
```

```text
Summarize an entity over a time window. <entity-type> e.g. ASSET / USER /
DOMAIN_NAME / IP_ADDRESS / FILE; <value> the identifier (hostname, user,
domain, IP, or hash). --json for the full summary.
```

**Flags**

| Flag | Type | Default | Description |
|---|---|---|---|
| `--hours` | int | 168 | look-back window in hours (168 = 7 days) |
