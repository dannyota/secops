# CBN parser template.
# Copy this file to parsers/<LOG_TYPE>.conf and keep the matching
# parsers/<LOG_TYPE>.yaml beside it.
filter {
  mutate {
    replace => {
      "metadata.event_type" => "GENERIC_EVENT"
      "metadata.vendor_name" => "Example"
      "metadata.product_name" => "Example Log"
    }
  }
}
